# 和AI对话初始化项目
```shell
请根据PRD.md、TECH_DESIGN.md和AGENTS.md的要求，
初始化一个React + TypeScript + Vite项目，并安装Tailwind CSS。

要求：
1. 创建完整的项目结构
2. 配置Tailwind CSS
3. 安装react-router-dom
4. 创建基础的路由配置
5. 确保项目能正常启动
```

# 创建故事数据
```shell
请根据PRD.md，创建stories.ts文件，包含3-5个海龟汤故事数据。

每个故事包含：
- id, title, difficulty, surface, bottom

要求：
1. 类型定义完整
2. 故事要有趣，难度分级
3. 导出stories数组
```

# 创建GameCard组件
```shell
请创建GameCard组件，显示单个游戏卡片：

要求：
1. 接收story作为props
2. 显示标题、难度标签
3. 卡片有hover效果
4. 点击卡片跳转到游戏页面
5. 使用Tailwind CSS，风格符合AGENTS.md
```
## 报错处理
```shell
项目报错了
[plugin:vite:import-analysis] Failed to resolve import "./common/FrostedCard" from "src\components\game\GameCard.tsx". Does the file exist?
```

# 创建Home页面
```shell
请创建Home页面，显示游戏大厅：

要求：
1. 显示页面标题"AI海龟汤"
2. 使用GameCard组件显示所有故事
3. 网格布局，响应式
4. 添加简单的介绍文字
5. 整体风格神秘悬疑
```

# 配置路由
```shell
请配置React Router：
1. / 路径显示Home页面
2. /game/:id 路径显示Game页面（先创建空组件）
```

# 创建Message组件
```shell
请创建Message组件，显示单条聊天消息：

要求：
1. 接收message作为props（role, content）
2. 用户消息在右边，AI消息在左边
3. 不同角色有不同的样式
4. 显示头像或图标
5. 使用Tailwind CSS
```

# 创建ChatBox组件
```shell
请创建ChatBox组件，实现聊天界面：

要求：
1. 上方显示消息列表（使用Message组件）
2. 下方有输入框和发送按钮
3. 输入框支持回车发送
4. 发送后清空输入框
5. 新消息自动滚动到底部
```

# 创建Game页面框架
```shell
请创建Game页面：

要求：
1. 根据URL参数获取当前故事
2. 顶部显示故事标题和汤面
3. 中间是ChatBox组件
4. 底部有"查看汤底"和"结束游戏"按钮
5. 整体布局合理，美观
```

# 封装AI API
```shell
请创建api.ts文件，封装AI调用：

要求：
1. 使用fetch或axios
2. 函数签名：askAI(question: string, story: Story): Promise<string>
3. 根据AGENTS.md中的Prompt模板组织请求
4. 处理API错误
5. API Key从环境变量读取
```
