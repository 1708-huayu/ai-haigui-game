# AI 海龟汤（多人派对版）产品需求文档 (PRD)

## 1. 产品概述
一款由 AI 驱动的多人在线情境推理（海龟汤）游戏。玩家可创建或加入虚拟房间，通过自然语言与扮演“主持人（法官）”的 AI 互动。游戏核心融入了社交共识机制（投票、权限管理），打造沉浸式的线上推理派对体验。

## 2. 目标用户与使用场景
* **推理/剧本杀爱好者**：寻找碎片化时间的逻辑推理代餐。
* **团队破冰/线上团建**：需要低门槛、强互动的多人在线互动工具。
* **休闲社交玩家**：下班后与好友连麦时的伴生休闲游戏。
* **AI 技术极客**：对大模型在复杂状态机和多 Agent 协同场景下的实际表现感兴趣的人群。

## 3. 核心功能拆解 (MVP 阶段)

### 3.1 游戏大厅与房间系统
* **内容流**：采用直观的卡片流展示海龟汤剧本，需标明标题、难度评级（如：入门、烧脑、诡异）、预估时长及当前热度。
* **房间机制**：
    * 玩家点击具体剧本后，选择“创建私人房间”或“快速匹配”。
    * **房主权限**：拥有“踢出玩家”的绝对权限（防止恶意捣乱刷屏的玩家干扰 AI 上下文）。
* **社交裂变**：支持生成房间邀请链接/口令，方便快速拉取微信/QQ好友。

### 3.2 核心对局区 (游戏页面)
* **信息展板**：置顶或侧边栏固定展示“汤面故事”及当前房间玩家列表。
* **公屏交互流**：
    * 玩家输入的问题及 AI 的回答（是/否/无关）以类似微信群聊的形式实时广播给所有房间成员。
    * 支持“线索高亮”：AI 判定为关键突破口的问题，在 UI 上给予特殊视觉标注。
* **共识决策机制（投票系统）**：
    * **触发条件**：任何非房主玩家点击“查看汤底”、“提前结束游戏”或“再来一局”时，触发全员投票弹窗。
    * **通过逻辑**：采用 **>50%（过半数）** 动态通过制。需考虑到掉线玩家的权重剔除机制（如：房间 4 人，1 人离线，则 2 人同意即可通过）。

### 3.3 结算与复盘 (汤底页面)
* **真相揭晓**：充满仪式感的动效展示完整“汤底”。
* **推理时间线（高光复盘）**：系统自动提取并展示本局游戏中，触发关键线索的玩家 ID 及对应提问，给予玩家正向成就感。
* **房间续存**：结算完成后，房间不解散，房主可通过“再来一局”直接返回大厅为当前房间挑选新剧本。

## 4. 界面设计规范 (UI/UX)
* **视觉基调**：暗黑科技风 (Dark Mystery)。以深蓝色调为主，营造悬疑感与专注感。
* **布局与材质**：
    * **大厅区**：建议将你提到的卡片式布局升级为现代感更强的 **Bento Box（便当盒）模块化排版**，错落有致地展示剧本和排行榜。
    * **游戏区**：采用 **Frosted Glass（磨砂玻璃）** 拟物化材质作为聊天气泡和面板的底色，既保证文字的高对比度易读性，又能透出深蓝色的神秘背景层，提升界面的质感与层次。

## 5. 技术架构选型建议

考虑到本版本引入了多人实时互动和投票状态机，原定的 Node.js 方案在处理复杂高并发状态时可能会遇到瓶颈，建议对技术栈进行如下微调，以保证工程的健壮性：

* **前端工程**：**React + TypeScript**。配合 Tailwind CSS 快速实现响应式和深色模式，使用 Zustand 或 Redux 管理复杂的房间状态流。
* **后端架构 (实时通信核心)**：推荐采用 **Java (Spring Boot)** 体系。
    * 使用 **Netty** 处理高并发的 WebSocket 长连接，保障公屏聊天的毫秒级同步。
    * 引入 **Redis** 作为高速缓存，处理高频的房间状态流转、玩家在线心跳检测以及投票计数原子操作。
    * 使用 **MySQL** 持久化存储剧本数据和历史对局记录。
* **AI 逻辑层 (工程难点)**：
    * **基座模型**：DeepSeek 或 Claude API（推理能力强）。
    * **编排框架**：强烈建议不要直接裸调 API。引入 **LangGraph** 构建 Multi-Agent 协作流。例如，设计一个专职的 `Judge Agent` 负责回答，一个 `State Agent` 负责评估玩家是否已猜出结局。
    * **状态持久化**：利用 Checkpointer 机制管理房间级别的多轮长对话状态，确保 AI 不会因为上下文过长而“忘记”之前的设定。
* **后续扩展 (剧本库)**：未来做海量自定义海龟汤时，可引入 **Milvus / Faiss 等向量数据库**，利用 RAG 技术让 AI 自动从庞大的剧本库中召回最匹配的情节，甚至实现动态剧情生成。